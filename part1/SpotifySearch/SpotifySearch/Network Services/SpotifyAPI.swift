//
//  SpotifyAPI.swift
//  SpotifySearch
//
//  Created by Chakane Shegog on 12/23/21.
//

import Foundation

struct SpotifyAPI {
    // this function takes in a search query of type string. this is what the user will enter in (input for endpoint)
    // it uses a completion handler to handle the network request as we compile our data
//    static func fetchRecipe(for searchQuery: String, completion: @escaping (Result<[Artists], AppError>) -> ()) {
//        
//    }
}
